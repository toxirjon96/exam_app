import 'package:exam_app/exam_app_library.dart';

class MacBooks extends StatelessWidget {
  const MacBooks({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("MacBooks"),
    );
  }
}
