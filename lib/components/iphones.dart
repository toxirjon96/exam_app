import 'package:exam_app/exam_app_library.dart';

class IPhones extends StatelessWidget {
  const IPhones({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("IPhones"),
    );
  }
}
